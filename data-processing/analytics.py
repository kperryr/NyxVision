import json
import yaml


#Methods to scan JSON array
score = 0

#Scan josn for words/phrases
def scanThreatData(arr):
    #pass first index
    #compare the keywords to the text in the report
    #if a match
        #create a keyword count array
        #add to score
    #return keyword array


def scanNegation(arr):
    #pass first index
    #compare the keywords to the text in the report
    #if a match
        #create a keyword count array
        #subtract from to score

def getScore():
    return score


#pass keyword count to add to enrichment object
#pass score to add to enrichment object